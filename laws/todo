- support dates without T
- support identifiers with numbers and _
- remove name value objects?
- typescript/c# private vars with _
- verify whether built in date functions work EXACTLY the same in dotnet and javascript. Many edge cases need to be confirmed.
- Fix inconsistencies in node name (sometimes value object without validation)
Add table validation
- wrong row value types
- invalid number of values
- TEST: multiple lookup calls with same signature
- ScenarioRunner.setParameter > check inner property types
- Scenario TABLE for values
- Refactor built in funciton scenarios so that the functions take values as parameters instead of default so it can be executed in UI
- UI: Improve state management, don't put everything is a single context
  - Implement backend
- Create npm, nuget packages



- Synchronisation error when removing old log files: Move remove to beforeAll

Summary of all failing tests
 FAIL  tests/parser/parseScenario.test.ts (7.23 s)
  ● ParseScenarioTests › testValidScenarioKeyword

    ENOENT: no such file or directory, unlink '/Users/timcools/_/Lexy/git/typescipt/tests/logs/20250114190540-lexy-parser.log'

      148 |       const hours = Math.abs(now - dateValue) / 36e5;
      149 |       if (hours > 1) {
    > 150 |         fs.unlinkSync(logFile);
          |            ^
      151 |       }
      152 |     }
      153 |   }

      at Function.unlinkSync [as removeOldFiles] (tests/loggingConfiguration.ts:150:12)
      at Function.removeOldFiles [as configure] (tests/loggingConfiguration.ts:63:26)
      at Function.configure [as getParserLogger] (tests/loggingConfiguration.ts:69:26)
      at getParserLogger (tests/parseFunctions.ts:16:39)
      at createParser (tests/parseFunctions.ts:24:18)
      at parseNodes (tests/parseFunctions.ts:54:27)
      at parseNode (tests/parseFunctions.ts:42:28)
      at Object.<anonymous> (tests/parser/parseScenario.test.ts:13:35)

  ● ParseScenarioTests › testValidScenario

    ENOENT: no such file or directory, unlink '/Users/timcools/_/Lexy/git/typescipt/tests/logs/20250114192455-lexy-parser.log'

      148 |       const hours = Math.abs(now - dateValue) / 36e5;
      149 |       if (hours > 1) {
    > 150 |         fs.unlinkSync(logFile);
          |            ^
      151 |       }
      152 |     }
      153 |   }

      at Function.unlinkSync [as removeOldFiles] (tests/loggingConfiguration.ts:150:12)
      at Function.removeOldFiles [as configure] (tests/loggingConfiguration.ts:63:26)
      at Function.configure [as getParserLogger] (tests/loggingConfiguration.ts:69:26)
      at getParserLogger (tests/parseFunctions.ts:16:39)
      at createParser (tests/parseFunctions.ts:24:18)
      at parseNodes (tests/parseFunctions.ts:54:27)
      at parseNode (tests/parseFunctions.ts:42:28)
      at Object.<anonymous> (tests/parser/parseScenario.test.ts:26:35)
